import { ThemeConfig } from "antd";

export const theme: ThemeConfig = {
  inherit: false,
  components: {
    Layout: {
      colorBgLayout: "#FFF",
    },
    Typography: {
      colorText: "#000",
      colorTextHeading: "#000",
      colorLink: "##000",
    },
  },
};
